# Supabase (required for live realtime bed board)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Optional: custom backend that implements PATCH /api/beds/:id/status
# If set, status updates go here instead of direct Supabase from the browser.
# VITE_API_URL=https://your-api.example.com
